{
  "name": "routeiq-parent",
  "version": "1.0.0",
  "description": "RouteIQ parent workspace coordinator",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "npx concurrently --kill-others \"npm run dev:server\" \"npm run dev:client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
